#include <stdio.h>
#include<string.h>
void main()
{
	 char c[20],d[20];
	int a,b;
	printf("\n enter the two strings");
	scanf("%s %s",&a,&b);
	a=strlen(c);
	b=strlen(d);
	if(c>=d)
	{
		printf("\n %s",c);
	}
	else
	{
	printf("\n %s",d);
	}
}
